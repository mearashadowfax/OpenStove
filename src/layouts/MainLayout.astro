---
import { Font } from 'astro:assets';
import Navbar from '@components/Navbar.astro';
import Footer from '@components/Footer.astro';
import Meta from '@components/Meta.astro';
import '@styles/global.css';

const {
  title = 'OpenStove',
  description,
  image,
  type,
  url,
  canonicalUrl,
  recipeData,
  robots,
} = Astro.props;

interface Props {
  title?: string;
  description?: string;
  image?: string;
  type?: 'website' | 'article';
  url?: string;
  canonicalUrl?: string;
  robots?: string;
  recipeData?: {
    author?: string;
    pubDate?: Date;
    cookingTime?: number;
    servings?: number;
    ingredients?: Array<{
      title?: string;
      items: Array<{
        quantity?: string;
        unit?: string;
        name: string;
      }>;
    }>;
    steps?: Array<{
      title?: string;
      actions: string[];
    }>;
  };
}

const currentUrl =
  url ||
  (canonicalUrl
    ? canonicalUrl.startsWith('http')
      ? canonicalUrl
      : new URL(canonicalUrl, Astro.site).href
    : Astro.url.href);
---

<html class="scroll-pt-16 scroll-smooth" lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{title}</title>
    <Font cssVariable="--font-figtree" preload />
    <Font cssVariable="--font-fraunces" preload />
    <Meta
      url={currentUrl}
      title={title}
      description={description}
      image={image}
      type={type}
      robots={robots}
      recipeData={recipeData}
    />
    <script is:inline>
      'dark' === localStorage.getItem('hs_theme') ||
      (!('hs_theme' in localStorage) &&
        window.matchMedia('(prefers-color-scheme: dark)').matches)
        ? document.documentElement.classList.add('dark')
        : document.documentElement.classList.remove('dark');
    </script>
    <style>
      body {
        overflow-y: scroll;
      }
      body::-webkit-scrollbar {
        width: 0.5rem;
      }
      @media (min-width: 1536px) {
        body::-webkit-scrollbar {
          width: 0.75rem;
        }
      }
      body::-webkit-scrollbar-track {
        background: var(--color-stone-100, #f5f5f4);
        border-radius: 9999px;
      }
      body::-webkit-scrollbar-thumb {
        background: var(--color-secondary);
        border-radius: 9999px;
      }
      .dark body::-webkit-scrollbar-track {
        background: var(--color-stone-700, #44403c);
      }
      .dark body::-webkit-scrollbar-thumb {
        background: var(--color-primary);
      }
    </style>
  </head>
  <body
    class="text-dark selection:bg-secondary dark:bg-dark dark:selection:bg-primary bg-orange-50 font-sans selection:text-stone-700 dark:text-stone-100 dark:selection:text-stone-700"
  >
    <a
      href="#main-content"
      class="focus:bg-secondary focus:text-dark sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:rounded-md focus:px-4 focus:py-2 focus:outline-hidden"
    >
      Skip to content
    </a>
    <div class="mx-auto max-w-(--breakpoint-2xl) px-4 md:px-8">
      <Navbar />
      <main id="main-content">
        <slot />
      </main>
    </div>
    <Footer />
    <script>
      import { HSStaticMethods } from 'preline';
      function initPreline() {
        HSStaticMethods.autoInit();
      }
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initPreline);
      } else {
        initPreline();
      }
    </script>
  </body>
</html>
